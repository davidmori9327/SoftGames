# Softgames Assignment — Pixi TypeScript

Run locally:

1. npm install
2. npm run dev

Open the dev url, choose a scene from the in-game menu, and allow fullscreen when prompted.

Features:
- Three tasks (Ace of Shadows, Magic Words, Phoenix Flame) accessible from menu
- Responsive fullscreen canvas
- FPS counter top-left
- TypeScript + Pixi v7
